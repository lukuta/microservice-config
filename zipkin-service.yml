configuration:  
  projectName: zipkin-service

server:
  port: 9411

eureka:  
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8090/eureka/
